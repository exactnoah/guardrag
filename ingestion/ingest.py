"""Main ingestion pipeline."""


def ingest_documents(source_path: str) -> list[dict]:
    """Load, chunk, and preprocess documents.
    
    Args:
        source_path: Path to source documents
        
    Returns:
        Processed document chunks
    """
    pass
