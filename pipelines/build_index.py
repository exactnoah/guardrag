"""Pipeline to build vector index from documents."""


def build_index(data_path: str, output_path: str, embedder=None):
    """Build vector index from source documents.
    
    Args:
        data_path: Path to source documents
        output_path: Path to save index
        embedder: Embedding model (optional)
    """
    pass
