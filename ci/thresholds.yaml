thresholds:
  faithfulness:
    min: 0.75
    description: "Minimum faithfulness score"
  
  retrieval_recall:
    min: 0.80
    description: "Minimum recall for retrieval"
  
  latency:
    max: 2000
    description: "Maximum latency in milliseconds"
  
  answer_length:
    min: 50
    max: 2000
    description: "Answer length bounds"
